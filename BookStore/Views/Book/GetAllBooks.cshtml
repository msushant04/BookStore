
@model IEnumerable<BookModel>;
@*Multiple Parameter send to Anchor Tag Helper as-All-route-data
        @{
        var parameters = new Dictionary<string, string>
        {
            {"Name","Sushant" },
            {"Id","1" },
            {"Designation","Software Engineer" }
        };
    }*@

<div class="container">
    <h1>All Books</h1>
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4">
               <partial name="_bookThumbnail" model="@item" />
                @*@Html.Partial("_bookThumbnail",@item)*@
            </div>
        }
    </div>
</div>
